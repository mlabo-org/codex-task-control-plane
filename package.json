{
  "name": "codex-session-control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm test",
    "smoke": "node scripts/control-plane-cli.mjs smoke",
    "dashboard": "node scripts/control-plane-cli.mjs serve"
  }
}
