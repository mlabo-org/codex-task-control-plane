interface:
  display_name: "Codex Task Control Plane"
  short_description: "Control visible Codex tasks with durable state"
  brand_color: "#2563EB"
  default_prompt: "Use $control-codex-tasks to create a dry-run task-control plan for visible Codex tasks."
dependencies:
  tools:
    - type: "mcp"
      value: "codex_task_control_plane"
      description: "Durable task intents, bindings, observations, decisions, and bilingual dashboard"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
